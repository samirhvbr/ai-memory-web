<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- The app's own database, in memory. The ai-memory index the tests
             exercise is a REAL file the test builds in a temp directory — see
             tests/Unit/AiMemory/AiMemoryDatabaseTest, which points the
             `aimemory` connection at it. The two are unrelated. -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- Deliberately empty: with no path configured the guard degrades, so
             a test that forgets to point it somewhere cannot accidentally read
             the developer's real ai-memory index. -->
        <env name="AI_MEMORY_SQLITE_PATH" value=""/>
        <env name="AI_MEMORY_TIMEZONE" value="UTC"/>
    </php>
</phpunit>
